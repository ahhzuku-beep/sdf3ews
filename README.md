Place your `warboy.png` here so the site can serve it at `/resources/warboy.png`.

Example:
- resources/warboy.png

Note: The image should have a transparent background if possible; the site will render it over a black background for a bold contrast. For best results use a high-contrast PNG sized around 800px wide (the site will scale it down).